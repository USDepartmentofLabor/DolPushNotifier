<%= simple_form_for @techbit do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :url , label: "URL" %>
  <%= f.input :active, as: :boolean %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
